<?xml version="1.0" encoding="ISO-8859-1"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.dao.BoardDAOImpl">

    <insert id="writeBoard" parameterType="com.project.dto.BoardDTO">
        INSERT INTO board(
            board_title,
            board_content,
            board_writer,
            board_hit,
            idx
        ) VALUES (
            #{btitle},
            #{bcontent},
            #{bwriter},
            0,
            #{idx}
        )
    </insert>

    <select id="viewBoard" parameterType="com.project.dto.BoardDTO" resultType="com.project.dto.BoardDTO">
        SELECT board_no as bno,
               board_title as btitle,
               board_writer as bwriter,
               board_hit as bhit,
               board_updatetime as bupdatetime,
               idx as idx
        FROM   board
    </select>

    <select id="readBoard" parameterType="com.project.dto.BoardDTO" resultType="com.project.dto.BoardDTO">
        SELECT board_title as btitle,
               board_writer as bwriter,
               board_content as bcontent,
               board_hit as bhit
        FROM   board
        WHERE  board_no = #{bno}
    </select>
</mapper>
